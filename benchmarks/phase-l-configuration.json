{
  "schemaVersion": 1,
  "name": "phase-l-configuration",
  "frozen": true,
  "frozenAt": "2026-09-19T16:40:00+08:00",
  "plan": "support/phase-l-configuration.md",
  "baseline": {
    "engineRevision": "2c1ceb3",
    "publishedPackage": "@fireside-dev/cli@0.1.0-next.8",
    "publishedEngineRevision": "03000f0cc082c1b69f06c67b6a99a5417c840243",
    "wrapperGate": "packages/cli/src/options.mjs loadProject: demo-* only, complete five-service profile only, storage array only, ui.enabled/singleProjectMode/firestore-array rejected, loopback only, unknown emulators.<name> a hard error",
    "engineGate": "suite-runtime validate_config: demo-* only, minimum_functions > 0, every listener unconditional, SuiteDirectory requires ui/logging/auth/functions/pubsub, host doubles as listen and connect address, rules keyed by project, export hardcodes (default)"
  },
  "toolchain": {
    "rust": "1.98.0",
    "node": "24.20.0",
    "firebaseToolsReference": "15.22.0"
  },
  "oracles": {
    "precedence": [
      "firebase-tools-15.22.0 emulator/controller.js, commandUtils.js, constants.js, env.js, utils.js connectableHostname, auth/server.js singleProjectMode, storage/rules/config.js",
      "firenook safety policy where the official CLI is silent (public bind warning)"
    ],
    "recorded": {
      "onlyDerivation": "filterEmulatorTargets: a top-level section or an emulators.<name> entry configures an emulator; --only filters without dependency expansion; zero targets = 'No emulators to start'",
      "demoStorageDefault": "no storage section on a demo project => templates/emulators/default_storage.rules (open) with the 'Detected demo project ID \u2026 default (open) rules configuration' line; a real project must configure rules",
      "demoBanner": "Detected demo project ID \"<id>\", emulated services will use a demo configuration and attempts to access non-emulated services for this project will fail.",
      "realProjectWarning": "The following emulators are not running, calls to these services from the Functions emulator will affect production: <list> (only when functions runs and only for real ids)",
      "hostPolicy": "0.0.0.0 and :: accepted without a warning; connectableHostname maps them to 127.0.0.1 / ::1 for the hub, the UI and every exported *_EMULATOR_HOST",
      "uiDisabled": "ui.enabled false also stops the logging emulator; the hub is unaffected",
      "singleProjectMode": "default true; the Auth emulator logs WARN 'Multiple projectIds are not recommended in single project mode\u2026' and keeps serving; never an error",
      "multipleDatabases": "the official Firestore emulator refuses a firestore array with more than one entry ('Cloud Firestore Emulator does not support multiple databases yet.') and then loads no rules"
    }
  },
  "gates": {
    "engine": {
      "unit": "suite-runtime, suite-front, cli and rules-runtime tests; a two-service --no-ui real-id 0.0.0.0 suite starts, lists only its listeners in /emulators with loopback addresses, warns once per foreign project id, writes the debug log and stops cleanly",
      "rustFmtRequired": true,
      "clippyWarningsAllowed": 0
    },
    "wrapper": {
      "unit": "packaging/cli.test.mjs: service derivation and --only errors, storage object form and absence, firestore arrays, ui/singleProjectMode flags, real ids with the warning, non-loopback hosts with the warning, --debug log path, exec --no-ui unless --ui"
    },
    "existingFixturesUnchanged": [
      "firebase-suite-v1",
      "functions-runtime-v1",
      "auth-v1",
      "pubsub-v1",
      "storage-rules-v1"
    ]
  },
  "acceptance": {
    "release": "0.1.0-next.9",
    "receipts": {}
  },
  "decisions": {
    "resumeState": {
      "policy": "--resume-state requires firestore, auth and storage (the native state receipt covers all three); subsets run with a fresh or in-memory state"
    },
    "publicBind": {
      "policy": "a non-loopback --host is accepted and announced with one warning line; the official CLI prints nothing"
    },
    "realProjectIds": {
      "policy": "accepted; the worker environment already sets every emulator host and strips GOOGLE_APPLICATION_CREDENTIALS, FIREBASE_TOKEN and CLOUDSDK_AUTH_CREDENTIAL_FILE_OVERRIDE and sets METADATA_SERVER_DETECTION=none; the launcher and the banner state that user code with explicit credentials can still reach the real project"
    },
    "multipleDatabases": {
      "policy": "a differentiator, not parity: every named database is served with its own rules (database-specific, then the project-wide hot-reload ruleset, then the default) and exported; the official emulator refuses the configuration"
    }
  },
  "receipts": {
    "l5": {
      "date": "2026-09-19",
      "candidate": "0.1.0-local.ged21d020ccfd (packaging/build-local.mjs on head ed21d02, installed as the consumer's local candidate)",
      "consumerGates": {
        "extensionsGate": "PASS: 16 custom + 3 extension backends, 24 admitted, 2 ignored by upstream (same as the official oracle capture), ready 7.8 s",
        "integration": "consumer integration gate 24/24",
        "installedSmoke": "consumer installed-launcher smoke PASS (default launcher; 21 Functions; two listeners; Unicode; Auth; gzip Storage both buckets; Pub/Sub; UI; export/shutdown; official-format import; dataset isolation; immutable seed protected)",
        "browserJourneys": "the consumer's nine browser journeys PASS on the macOS full-data stack with external providers disabled: otp-auth-login 5047 ms, dashboard-and-deck-list 1276, existing-deck-and-listener-edit 15123, catalog-slide-add 24503, deck-image-upload 14117, duplicate-and-delete-deck 4969, dotnet-deck-export 8731, dev-admin-pages 15258, sign-out-and-sign-in 4282; 0 page errors, 0 failures among 6127 required first-party requests (7059 first-party responses, 8 WebSocket connections); the persistent full-data working state reopened on the candidate and the suite stopped cleanly (16 functions, 2 schedules, 0 task queues, 211,257 documents, 1 Auth user, 33,354 objects)"
      },
      "syntheticEndToEnd": "a fresh project scaffolded by `firenook init` with firebase-functions 7.4.0 (Express 5) on the packed candidate: helloWorld over HTTP, functions:invoke for an HTTPS function, a Firestore document event and a task-queue payload, getFunctions().taskQueue().enqueue() from a function reaching the handler, /queueStats, firestore:delete (document, shallow refused without -r, recursive with -f), emulators:export, use, target:apply, a second Firestore database (rules line per database, reads on both), firenook mcp (19 tools: status, query, cross-database get, auth_create_user, functions_list, tasks_stats), init --adopt on a hosting-bearing firebase.json (skip + additions), a real project id on --host 0.0.0.0 with the official banners and loopback connect addresses",
      "pairedAcceptance": "the per-release-line 2 h soak stands from next.6/next.7; the consumer gates and journeys above ran on this candidate",
      "pending": [
        "consumer pin bump with the extensions gate on the installed package"
      ],
      "exactCandidateCi": {
        "headSha": "7fb0c350078c94fc65a83a6f3c012a917779fbbd",
        "conclusion": "success",
        "runs": {
          "CI": 35437156565,
          "Installable packages": 35437156569
        },
        "jobs": [
          "Rust quality gate",
          "Public fixture and package checks",
          "Differential harness",
          "Firebase JS SDK browser integration (memory, client memory)",
          "Firebase JS SDK browser integration (memory, client persistence)",
          "Firebase JS SDK browser integration (disk-wal, client memory)",
          "Firebase JS SDK browser integration (disk-wal, client persistence)",
          "Packed install (linux-x64)",
          "Packed install (linux-arm64)",
          "Packed install (darwin-arm64)",
          "Packed install (darwin-x64)",
          "Packed install (win32-x64)",
          "verify"
        ],
        "note": "the Cloud Tasks replay, the Functions runtime corpus, the Auth and Pub/Sub replays and the Pub/Sub client-library check run inside the Differential harness job; the engine sources at this head equal ed21d02 (docs-only commits after it)"
      },
      "publication": {
        "version": "0.1.0-next.9",
        "tag": "npm-v0.1.0-next.9",
        "taggedCommit": "e9c58e0f2dcf8cbaa47490085f6389e6ea4363cd",
        "engineRevision": "7fb0c350078c94fc65a83a6f3c012a917779fbbd",
        "releaseRun": 35440801335,
        "publishedAt": "2026-09-19T12:08:25.507Z",
        "registry": "registry integrity (sha512 and sha1) equal to the release assets for all six packages; npm audit signatures on a fresh install; GitHub prerelease with latest=false",
        "distTags": "next -> 0.1.0-next.9; latest untouched (stable promotion is a separate decision)"
      }
    }
  }
}
